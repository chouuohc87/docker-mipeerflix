version: "3.9"

services:
    chisel:
        container_name: chisel-client
        image: jpillora/chisel:latest
        command:
            - client
            - --auth=ubuntu:ubuntu
            - https://85ptc4-5000.sse.codesandbox.io
            - 1080:socks
        restart: unless-stopped
    mipeerflix:
        container_name: mipeerflix
        build:
            context: .
        ports:
            - "chisel:1080:1080"
            - "chisel:1080:1080/udp"
        restart: unless-stopped
